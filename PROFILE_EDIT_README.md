# 프로필 수정 화면 구현 완료 ✅

## 📁 생성된 파일

- `app/(screens)/profile-edit.tsx` - 프로필 수정 화면

## 🔄 수정된 파일

- `contexts/AuthContext.tsx` - `updateProfile` 함수 추가
- `app/(tabs)/profile.tsx` - 프로필 수정 메뉴 연결

## 🎯 구현된 기능

### 1. **이름 변경**

- ✅ 실시간 유효성 검증 (최소 2자)
- ✅ 글자 수 표시 (최대 50자)
- ✅ 빈 값 방지
- ✅ X 버튼으로 입력값 초기화

### 2. **프로필 사진 변경**

- ⚠️ **UI만 구현됨** (expo-image-picker 미설치)
- 버튼 클릭 시 설치 안내 메시지 표시
- 이미지 선택 기능은 라이브러리 설치 후 활성화 가능

### 3. **변경사항 추적**

- ✅ 변경사항 있을 때만 저장 버튼 활성화
- ✅ 변경사항 안내 배너 표시
- ✅ 취소 시 확인 대화상자

### 4. **이메일 표시**

- ✅ 읽기 전용 (변경 불가)
- ✅ 비활성화 스타일 적용

### 5. **소셜 로그인 배지**

- ✅ 카카오 로그인 사용자 표시

### 6. **데이터 저장**

- ✅ AuthContext의 updateProfile 함수 사용
- ✅ AsyncStorage에 즉시 반영
- ✅ 프로필 화면 자동 업데이트

## 📱 화면 구성

```
┌─────────────────────────────────┐
│  ← 프로필 수정                   │
│  이름과 프로필 사진을 변경하세요  │
├─────────────────────────────────┤
│                                 │
│         [프로필 사진]            │
│         📷 카메라 버튼           │
│      탭하여 사진 변경            │
│                                 │
│  이름                            │
│  👤 [홍길동____________]  ✖      │
│  25/50 ✓                        │
│                                 │
│  이메일                          │
│  [user@example.com_____]        │
│  이메일은 변경할 수 없습니다      │
│                                 │
│  [카카오 계정]                   │
│                                 │
│  ✓ 저장되지 않은 변경사항이      │
│    있습니다                      │
│                                 │
├─────────────────────────────────┤
│  [취소]          [저장하기]      │
└─────────────────────────────────┘
```

## 🚀 사용 방법

### 1. 프로필 화면에서 접근

```typescript
// app/(tabs)/profile.tsx
프로필 탭 → "프로필 수정" 메뉴 클릭
```

### 2. 이름 변경

```
1. 이름 입력란에 새 이름 입력
2. 최소 2자 이상 입력 시 "✓" 표시
3. "저장하기" 버튼 클릭
4. 성공 메시지 확인 후 자동으로 프로필 화면 복귀
```

### 3. 프로필 사진 변경 (라이브러리 설치 필요)

```bash
# expo-image-picker 설치
npx expo install expo-image-picker

# 설치 후 자동으로 활성화됨
```

## 🔧 API 연동 가이드

### 현재 상태

- ✅ UI 완성
- ⚠️ Mock 데이터 사용 (로컬 스토리지)
- ⚠️ 실제 서버 API 미연동

### 연동 방법

#### 1. AuthContext.tsx 수정

```typescript
const updateProfile = async (data: {
  name?: string;
  avatar?: string | null;
}) => {
  if (!user) return;

  try {
    // ✅ 이 부분의 주석을 해제하고 실제 API 호출
    const response = await fetch("/api/v1/users/me", {
      method: "PATCH",
      headers: {
        Authorization: `Bearer ${token}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        name: data.name,
        avatar: data.avatar, // base64 또는 URL
      }),
    });

    if (!response.ok) throw new Error("Update failed");

    const result = await response.json();

    // 서버 응답으로 사용자 정보 업데이트
    setUser(result.data);
    await saveUserToStorage(result.data);
  } catch (error) {
    console.error("Failed to update profile:", error);
    throw error;
  }
};
```

#### 2. 이미지 업로드 처리

```typescript
// profile-edit.tsx의 handleSave 함수 수정

// 이미지를 base64로 변환 (선택 1)
const imageBase64 = await FileSystem.readAsStringAsync(avatar, {
  encoding: FileSystem.EncodingType.Base64,
});

// 또는 FormData로 전송 (선택 2)
const formData = new FormData();
formData.append("name", name);
formData.append("avatar", {
  uri: avatar,
  type: "image/jpeg",
  name: "avatar.jpg",
} as any);
```

## 📋 API 명세 (백엔드 구현 필요)

### PATCH /api/v1/users/me

프로필 정보 수정

**요청 헤더**

```
Authorization: Bearer {access_token}
Content-Type: application/json
```

**요청 바디**

```json
{
  "name": "홍길동",
  "avatar": "base64_encoded_image_data" // 또는 null
}
```

**응답 (성공)**

```json
{
  "success": true,
  "data": {
    "id": "usr_123",
    "email": "user@example.com",
    "name": "홍길동",
    "avatar": "https://cdn.example.com/avatars/usr_123.jpg",
    "updatedAt": "2026-01-22T10:30:00Z"
  }
}
```

**응답 (에러)**

```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "이름은 2자 이상이어야 합니다"
  }
}
```

## ⚠️ 주의사항

### 1. expo-image-picker 미설치

- 현재 이미지 선택 기능은 비활성화 상태
- 설치 명령어: `npx expo install expo-image-picker`
- 설치 후 주석 해제 필요

### 2. 이미지 크기 제한

- 권장: 5MB 이하
- 프론트에서 압축 처리 필요
- 서버에서도 검증 필수

### 3. 네트워크 에러 처리

- 재시도 로직 구현됨
- 3회 실패 시 사용자에게 안내

### 4. 권한 처리

- 갤러리 접근 권한 필요
- 카메라 권한 필요 (사진 촬영 시)
- iOS/Android 각각 설정 필요

## 📦 필요한 패키지

### 현재 설치됨

- ✅ expo-router
- ✅ react-native-reanimated
- ✅ expo-linear-gradient
- ✅ lucide-react-native
- ✅ @react-native-async-storage/async-storage

### 추가 설치 필요

```bash
# 이미지 선택 기능
npx expo install expo-image-picker

# 이미지 압축 (선택)
npx expo install expo-image-manipulator
```

## 🎨 디자인 시스템

- **컬러**: Colors.emerald (프라이머리), Colors.zinc (배경/텍스트)
- **폰트**: FontWeight.bold, FontWeight.semibold
- **간격**: Spacing.sm/md/lg/xl
- **둥근 모서리**: BorderRadius.xl/2xl/full
- **애니메이션**: FadeInUp (진입 시)

## 🔄 업데이트 플로우

```
1. 사용자가 이름 입력
   ↓
2. hasChanges = true
   ↓
3. "저장하기" 버튼 활성화
   ↓
4. 버튼 클릭
   ↓
5. 로딩 상태 (버튼 비활성화)
   ↓
6. AuthContext.updateProfile() 호출
   ↓
7. AsyncStorage 업데이트
   ↓
8. 프로필 화면 자동 업데이트
   ↓
9. 성공 메시지 표시
   ↓
10. 이전 화면으로 복귀
```

## 🐛 알려진 이슈

### 1. 이미지 선택 미구현

- **원인**: expo-image-picker 미설치
- **해결**: `npx expo install expo-image-picker` 실행

### 2. 서버 API 미연동

- **원인**: 백엔드 미구현
- **현재**: Mock 데이터로 로컬 동작
- **해결**: API 구현 후 AuthContext 수정

## 📝 다음 단계

1. [ ] expo-image-picker 설치
2. [ ] 이미지 압축 로직 추가
3. [ ] 백엔드 API 구현
4. [ ] API 연동 및 토큰 관리
5. [ ] 에러 핸들링 강화
6. [ ] 이미지 캐싱 최적화
7. [ ] 테스트 코드 작성

## 🎯 테스트 시나리오

### 정상 플로우

1. ✅ 프로필 탭 → "프로필 수정" 클릭
2. ✅ 이름 변경 (예: "테스터" → "러너")
3. ✅ "저장하기" 버튼 클릭
4. ✅ 성공 메시지 확인
5. ✅ 프로필 화면에서 변경 확인

### 에러 플로우

1. ✅ 이름을 1자만 입력 → 저장 버튼 비활성화
2. ✅ 이름을 비워둠 → 저장 버튼 비활성화
3. ✅ 변경 없이 저장 시도 → 저장 버튼 비활성화
4. ✅ 취소 버튼 클릭 → 확인 대화상자
5. ✅ 프로필 사진 버튼 → 안내 메시지

## 📞 문의

버그 리포트 및 기능 제안:

- GitHub Issues
- 팀 Slack 채널

---

**작성일**: 2026년 1월 22일  
**버전**: 1.0.0  
**작성자**: AI Assistant
